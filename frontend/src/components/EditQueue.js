import React, { Fragment } from "react";

const EditQueue = () => {
    return (
        <Fragment>
            <h1 className="text-center mt-5 small">Edit Queue</h1>
        </Fragment>
    );
};

export default EditQueue;